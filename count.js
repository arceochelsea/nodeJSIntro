var counter = function (arr){
    return 'There are ' + arr.length + ' elements in this array';
};

module.exports = counter; //this is what is returned to us when we require a module
// setting this equal to what we want avail outside of this module